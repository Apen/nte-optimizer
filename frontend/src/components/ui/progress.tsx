import * as ProgressPrimitive from '@radix-ui/react-progress'
export function Progress({ value }: { value?: number }) { return <ProgressPrimitive.Root value={value} className="relative h-2 w-full overflow-hidden rounded-full bg-slate-800"><ProgressPrimitive.Indicator className="h-full w-full bg-pink-500 transition-transform" style={{ transform: `translateX(-${100-(value ?? 35)}%)` }} /></ProgressPrimitive.Root> }
